{
    "version": 2,
    "builds": [
      {
        "src": "/landingPage/package.json",
        "use": "@vercel/static-build",
        "config": { "distDir": "dist" }
      },
      {
        "src": "/adminPanel/package.json",
        "use": "@vercel/static-build",
        "config": { "distDir": "build" }
      }
    ],
    "routes": [
      { "src": "/admin/(.*)", "dest": "TimeLoop-FrontEnd/adminPanel/$1" },
      { "src": "/(.*)", "dest": "TimeLoop-FrontEnd/landingPage/$1" }
    ]
  }
  